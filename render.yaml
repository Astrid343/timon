services:
  - type: web
    name: timon
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python timon.py"
    port: 5000  # Убедитесь, что здесь указан правильный порт
